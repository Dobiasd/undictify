version: '2.4'

services:

  unit-tests-3-6:
    build:
      context: .
      dockerfile: Dockerfile_3.6
    volumes:
     - .:/undictify
    working_dir: /undictify
    entrypoint: ./helpers/run_code_checks.sh

  unit-tests-3-7:
    build:
      context: .
      dockerfile: Dockerfile_3.7
    volumes:
     - .:/undictify
    working_dir: /undictify
    entrypoint: ./helpers/run_code_checks.sh
